{
  "name": "Atribuição P3",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "forwardPorts": [5000],
  "portsAttributes": {
    "5000": {
      "label": "Aplicação",
      "onAutoForward": "openBrowser"
    }
  },
  "postCreateCommand": "pip install -r requirements.txt && sudo apt-get update && sudo apt-get install -y libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 libffi-dev libcairo2",
  "postStartCommand": "python run.py",
  "customizations": {
    "codespaces": {
      "openFiles": ["README.md"]
    }
  }
}
